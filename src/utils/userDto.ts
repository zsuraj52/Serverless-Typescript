export default interface inputUserDto {
    username:string;
    email: string;
    password: string;
}

export default interface updatedUserDto {
    id:string;
    username:string;
    email: string;
    createdAt:string;
    updatedAt: string;
}